{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "npm run lint 2>&1 || { echo '{\"decision\":\"block\",\"reason\":\"Lint failed. Fix lint errors before committing.\"}'; exit 1; }",
            "timeout": 30,
            "statusMessage": "Running lint..."
          }
        ]
      }
    ]
  }
}
